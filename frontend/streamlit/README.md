# CodeMentor AI — Streamlit Frontend Demo

This is a frontend-only Streamlit demo app built for hackathon presentations.

Run:

```bash
pip install -r frontend/streamlit/requirements.txt
streamlit run frontend/streamlit/app.py
```

This demo is mock-only (no backend, no authentication, no real AI).
